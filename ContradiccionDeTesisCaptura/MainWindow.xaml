<Window x:Class="ContradiccionDeTesisCaptura.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:igDP="http://infragistics.com/DataPresenter"
        xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
        xmlns:converter="clr-namespace:ContradiccionDeTesisCaptura.Converters"
        Title="MainWindow"
        Width="1005"
        Height="404"
        Loaded="Window_Loaded">
    <Window.Resources>
        <converter:StatusConverter x:Key="statusConverter" />
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <Button Name="BtnAgregar"
                Grid.Row="2"
                Width="75"
                Height="23"
                Margin="0,0,26,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Click="BtnAgregar_Click"
                Content="Agregar" />
        <Button Name="BtnModificar"
                Grid.Row="2"
                Width="75"
                Height="23"
                Margin="0,0,107,0"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Click="BtnModificar_Click"
                Content="Modificar" IsEnabled="False" />
        <telerik:RadGridView Name="RGridContradicciones"
                             Grid.Row="1"
                             Margin="25,25,25,25"
                             HorizontalAlignment="Stretch"
                             VerticalAlignment="Stretch"
                             AutoGenerateColumns="False"
                             CanUserDeleteRows="False"
                             CanUserInsertRows="False"
                             IsReadOnly="True"
                             ItemsSource="{Binding}"
                             ShowGroupPanel="False">
            <telerik:RadGridView.Columns>
                <telerik:GridViewDataColumn Width="40"
                                            DataMemberBinding="{Binding IdContradiccion}"
                                            Header="Id"
                                            IsVisible="True" />
                <telerik:GridViewDataColumn Width="80"
                                            DataMemberBinding="{Binding ExpedienteNumero}"
                                            Header="Num. Exp."
                                            IsVisible="True" />
                <telerik:GridViewDataColumn Width="Auto"
                                            DataMemberBinding="{Binding ExpedienteAnio}"
                                            Header="Año"
                                            IsFilterable="True" />
                <telerik:GridViewDataColumn Width="Auto"
                                            DataMemberBinding="{Binding IdTipoAsunto}"
                                            Header="Tipo de Asunto"
                                            IsFilterable="True" />
                <telerik:GridViewDataColumn Width="*"
                                            DataMemberBinding="{Binding Tema}"
                                            Header="Tema" />
                <telerik:GridViewDataColumn Width="100"
                                            DataMemberBinding="{Binding Status,Converter={StaticResource statusConverter}}"
                                            Header="Estado" />
                <telerik:GridViewDataColumn Width="150"
                                            DataMemberBinding="{Binding Oficio}"
                                            Header="Oficio"
                                            IsFilterable="False" />
                <telerik:GridViewDataColumn Width="150"
                                            DataMemberBinding="{Binding FechaTurno}"
                                            DataFormatString=" {0: dd/MM/yyyy} "
                                            Header="Fecha de turno"
                                            IsFilterable="True" />
                <telerik:GridViewDataColumn Width="150"
                                            DataMemberBinding="{Binding Denunciantes}"
                                            Header="Denunciante" />


            </telerik:RadGridView.Columns>
        </telerik:RadGridView>
        <Button Content="Visualizar" Grid.Row="2" Height="23" HorizontalAlignment="Left" Margin="720,0,0,0" Name="BtnVisualizar" VerticalAlignment="Top" Width="75" Click="BtnVisualizar_Click" />
    </Grid>
</Window>
